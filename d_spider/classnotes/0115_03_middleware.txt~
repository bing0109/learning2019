
关闭某些默认开启的模块
settings.py里面相应位置设置为None
    eg:关闭连不上自动重试（3次）的模块
    DOWNLOADERMIDDLEWARE = {
        'scrapy.downloadermiddlewares.retry.RetryMiddleware': None,
    }


    def process_request(self, request, spider):
        return None         # 加代理，加请求头，cookie等
        return request
        return response     # 和selenium对接
    
    def process_response(self, request, response, spider):
        return None         
        return request      # 检查响应等
        return response     # 文件解压、检查响应状态

    def process_exception(self, request, exception, response):
        return None         # 错误重试，处理一次
        return request      # 错误重试，处理多次
        return response
        

